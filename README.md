# Docling Endpoint

This is a simple example of shipping a Docling Endpoint via Databricks model serving. It has been tested on Serverless v4, CPU and GPU model serving, and via Databricks Asset Bundles. 
